version: '3.8'

services:
  voicecheck:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - .:/app
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
