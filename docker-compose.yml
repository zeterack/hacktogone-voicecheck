version: '3.8'

services:
  voicecheck:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./.streamlit/secrets.toml:/app/.streamlit/secrets.toml
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
